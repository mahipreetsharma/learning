Arrays
    Collection of data which are of same data type

    when we declare a array the array whill be stored in stack in Main Memory
    int a[5]; declaration of array
    initialization of array
    int a[5] = {1,2,3,4,5};

    we use loops to scan or traverse through the array to access the element present in array
    we can skip the size of the array while initializing

    sizeof will provide the logical size of array arr[3] = {1,2,3} will have size of 21

Structure
    struch make a structure for the code
    struct rectangle{ delc
        int length;
        int breath;
    }
        int main(){
            struct recctangle r declaratoion
            r.length = 10;
            r.breath = 12;
        }
        
Pointer
    a adress variable store only the address of data, it use to access data inderectly
    to access heap memory we use pointer
    pointer also use for parameter passing

    example for data pinting-
    int a = 10; data variable;
    int *p; declaration
    p = &a; initialization of pinter this will have address of data variable
    printf("%d",*p)

    example for heap memory alocating-
    use malloc t access the heap memory
    int main(){
        int *p;
        p = (int *)malloc(5*sizeof(int)); for c
        p = new int(5); cpp code to create heap memory
    }

    every data in pointer allocate same size of memory 

Reference
    it is used to access tha value 
    example 
    int a = 10;
    int &r = a;// reference to a , it wil also change the value of a when we update the value of r
    use for parameter passing

Pointer to Structure
    struct rectangle *p = &a;
    a.length =10;     }
    (*p).length = 10; these 3 are the syntax for the pointer for structure
    p->length =10;  }

    heap memory for pointer structure
    struct recctangle *p;
    p=(struct recctangle *)malloc(sizeof(struct recctangle)) //heap memory for the pointer structure in c
    p->length= 10;
    p->breath = 12;
    
functions
    fuction is set of instructions of code to perform a specific task.
    functions are used for code reusability
    example-
        int add(int a,int b){ //defining of functions   
            int c = a+b;
            return c;
        }

        int main(){
            int x =10;
            int y = 20;
            int z = add(x,y); // intialization of function
            cout<<z;  
        }